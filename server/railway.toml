[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app && python -m server.scripts.ingest && uvicorn server.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
